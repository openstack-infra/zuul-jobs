- name: Install Fedora repository files
  become: yes
  template:
    dest: "/{{ item }}"
    group: root
    mode: 0644
    owner: root
    src: "{{ item }}.j2"
  with_items:
    - etc/yum.repos.d/fedora.repo
    - etc/yum.repos.d/fedora-updates.repo
  notify:
    - Update dnf cache
